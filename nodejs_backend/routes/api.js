/**
 * API routes (alternative structure)
 * Currently using inline routes in server.js
 * This file can be used for route organization if needed
 */

module.exports = (app) => {
  // Routes can be defined here if needed
};

